name: flutter_pixel
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  image_gallery_saver: ^2.0.3 
  image_picker: ^0.8.4+4
  flutter_painter: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0



flutter:
  uses-material-design: true
  assets:
    - assets/    # Adicione esta linha para incluir todo o conteúdo do diretório assets
